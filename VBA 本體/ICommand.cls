Option Explicit
' ==========================================================
' MODULE: ICommand (類別模組 - 介面)
' PURPOSE: 命令物件介面合約。強制規定全系統所有具體管線指令，必須實作接收強型態 Context 的 Execute 方法與 DAG 排程屬性。
' EXPORTS: CommandName, Dependencies, IsCritical, Execute
' IMPORTS: cls_ExecutionContext
' FORBIDDEN: 嚴禁在此模組撰寫任何具體的業務邏輯實作，100% 維持純粹的介面合約宣告。
' DEPENDENCIES: 無外部依賴
' VERSION: 1.0.0 [Stability: Stable]
' ==========================================================
Private Const MODULE_NAME As String = "ICommand"
Private Const MODULE_VERSION As String = "1.0.0"

' ------------------------------------------------------------------------------
' 屬性名稱：CommandName (唯讀)
' 功用與目的：定義實體命令的唯一識別名稱 (例如 "IMPORT", "RESCUE", "REBUILD")。
' 為什麼要這樣判斷/設計：作為合約的一部分，讓底層 DAG 引擎能夠精準識別、比對並註冊該命令，杜絕管線中出現重複或遺漏的執行節點。
' ------------------------------------------------------------------------------
Public Property Get CommandName() As String
    ' 由具體命令類別實作：回傳該命令的唯一識別字串
End Property
' ------------------------------------------------------------------------------
' 屬性名稱：Dependencies (唯讀)
' 功用與目的：定義在執行本命令之前，必須先完成的其他「前置命令名稱」陣列。
' 為什麼要這樣判斷/設計：這是 DAG (有向無環圖) 引擎排程與拓撲排序的絕對依據，能確保業務邏輯的先後順序正確，並在管線啟動前主動攔截死鎖 (Deadlock)。
' ------------------------------------------------------------------------------
Public Property Get Dependencies() As Variant
    ' 由具體命令類別實作：回傳包含依賴名稱的一維字串陣列；若無依賴則回傳 Array()
End Property
' ------------------------------------------------------------------------------
' 屬性名稱：IsCritical (唯讀)
' 功用與目的：定義此命令在系統中的「關鍵安全級別」。
' 為什麼要這樣判斷/設計：落實防禦性編程。若回傳 True 且該命令在執行中發生崩潰，管線控制器 (Mod_Pipeline) 必須立刻觸發全系統熔斷機制並執行自癒收網，防止後續命令繼續執行導致資料損毀。
' ------------------------------------------------------------------------------
Public Property Get IsCritical() As Boolean
    ' 由具體命令類別實作：回傳布林值，宣告自身是否為核心熔斷節點
End Property
' ------------------------------------------------------------------------------
' 方法名稱：Execute
' 功用與目的：執行命令的實體業務邏輯管線。
' 參數說明：Context (cls_ExecutionContext) - 傳入全域唯一的動態狀態物件與資源。
' 為什麼要這樣判斷/設計：強制所有命令統一透過強型別的 Context 取得參數、設定與日誌庫，徹底杜絕全域變數 (Global Variables) 造成的狀態污染與耦合。
' ------------------------------------------------------------------------------
Public Sub Execute(ByVal Context As cls_ExecutionContext)
    ' 由具體命令類別實作：提取 Context 資源，執行實體業務操作
End Sub
